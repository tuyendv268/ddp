general:
  batch_size: 4
  device: cpu
  mode: train
  epoch: 2
  # cross = dual
  model_type: "cross"
  valid_size: 0.1
  test_size: 0.1
  max_length: 384
  n_worker: 0
  save_ckpt_per_n_epoch: 1 
  # envibert
  plm: xlmr
  accumulation_steps: 2

path: 
  pretrained_dir: pretrained
  train_data: data/testset.json
  test_data: data/testset.json
  warm_up: checkpoints/cross_1.bin
  ckpt: outputs/checkpoints
  log: outputs/logs
  bm25: outputs/bm25
  embedd_model: outputs/checkpoints/dual_0.bin

data_augmentation_args:
  augmentation_percent: 0.6
  mask_percent: 0.05
